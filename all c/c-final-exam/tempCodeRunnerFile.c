int number = 50;
    // int num2 = 60;
    // int *p;
    // int *ptr;
    // p = &number;
    // ptr = &num2;
    // printf("Address of p variable is %p %o \n", p, ptr);
    // printf("Value of p variable is %d %d \n", *p, *ptr);
